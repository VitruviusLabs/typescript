const enum BaseType
{
	NULLISH = "nullish",
	BOOLEAN = "boolean",
	INTEGER = "integer",
	REAL = "real",
	INFINITY = "infinity",
	BIG_INT = "bigint",
	STRING = "string",
	SYMBOL = "symbol",
	ARRAY = "array",
	RECORD = "record",
	INSTANTIATED = "instantiated",
	CALLABLE = "callable",
	CONSTRUCTIBLE = "constructible",
}

export { BaseType };
