/**
 * Useful numbers for extracting date and time from ISO string
 *
 * @deprecated Use DateTimeFormatEnum instead
 */
const enum DateEnum
{
	ISO_DATE_LENGTH = 10,
	ISO_TIME_OFFSET = 11,
	ISO_DATETIME_LENGTH = 19,
}

export { DateEnum };
