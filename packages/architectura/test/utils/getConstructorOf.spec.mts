import { describe, it } from "node:test";

import { getConstructorOf } from "../../src/_index.mjs";

describe(
    "getConstructorOf",
    (): void =>
    {
        it.todo(
            "should be tested",
            (): void =>
            {
                getConstructorOf;
            }
        );
    }
);
