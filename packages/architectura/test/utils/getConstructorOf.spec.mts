import { describe, it } from "node:test";

import { getConstructorOf } from "../../src/index.mjs";

describe(
    "getConstructorOf",
    (): void =>
    {
        it.todo(
            "should be tested",
            (): void =>
            {
                getConstructorOf;
            }
        );
    }
);
