import { describe, it } from "node:test";

import { SessionPreHook } from "../../src/_index.mjs";

describe(
    "SessionPreHook",
    (): void =>
    {
        it.todo(
            "should be tested",
            (): void =>
            {
                SessionPreHook;
            }
        );
    }
);
