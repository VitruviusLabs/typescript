import { describe, it } from "node:test";

import { CookiePreHook } from "../../src/index.mjs";

describe(
    "CookiePreHook",
    (): void =>
    {
        it.todo(
            "should be tested",
            (): void =>
            {
                CookiePreHook;
            }
        );
    }
);
