import { describe, it } from "node:test";

import { BasePreHook } from "../../src/index.mjs";

describe(
    "BasePreHook",
    (): void =>
    {
        it.todo(
            "should be tested",
            (): void =>
            {
                BasePreHook;
            }
        );
    }
);
