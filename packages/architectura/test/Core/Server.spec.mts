import { describe, it } from "node:test";

import { Server } from "../../src/_index.mjs";

describe(
    "Server",
    (): void =>
    {
        it.todo(
            "should be tested",
            (): void =>
            {
                Server;
            }
        );
    }
);
