import { describe, it } from "node:test";

import { BasePreHook } from "../../../src/_index.mjs";

describe(
    "BasePreHook",
    (): void =>
    {
        it.todo(
            "should be tested",
            (): void =>
            {
                BasePreHook;
            }
        );
    }
);
