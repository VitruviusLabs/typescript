import { describe, it } from "node:test";

import { BasePostHook } from "../../../src/_index.mjs";

describe(
    "BasePostHook",
    (): void =>
    {
        it.todo(
            "should be tested",
            (): void =>
            {
                BasePostHook;
            }
        );
    }
);
