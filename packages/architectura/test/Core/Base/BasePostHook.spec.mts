import { describe, it } from "node:test";

import { BasePostHook } from "../../../src/index.mjs";

describe(
    "BasePostHook",
    (): void =>
    {
        it.todo(
            "should be tested",
            (): void =>
            {
                BasePostHook;
            }
        );
    }
);
