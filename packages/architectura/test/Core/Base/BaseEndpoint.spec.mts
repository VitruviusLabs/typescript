import { describe, it } from "node:test";

import { BaseEndpoint } from "../../../src/_index.mjs";

describe(
    "BaseEndpoint",
    (): void =>
    {
        it.todo(
            "should be tested",
            (): void =>
            {
                BaseEndpoint;
            }
        );
    }
);
