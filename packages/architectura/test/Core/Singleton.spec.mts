import { describe, it } from "node:test";

import { Singleton } from "../../src/index.mjs";

describe(
    "Singleton",
    (): void =>
    {
        it.todo(
            "should be tested",
            (): void =>
            {
                Singleton;
            }
        );
    }
);
