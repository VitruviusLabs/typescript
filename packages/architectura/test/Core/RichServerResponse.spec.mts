import { describe, it } from "node:test";

import { RichServerResponse } from "../../src/_index.mjs";

describe(
    "RichServerResponse",
    (): void =>
    {
        it.todo(
            "should be tested",
            (): void =>
            {
                RichServerResponse;
            }
        );
    }
);
