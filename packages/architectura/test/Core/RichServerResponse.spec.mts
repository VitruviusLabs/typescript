import { describe, it } from "node:test";

import { RichServerResponse } from "../../src/index.mjs";

describe(
    "RichServerResponse",
    (): void =>
    {
        it.todo(
            "should be tested",
            (): void =>
            {
                RichServerResponse;
            }
        );
    }
);
