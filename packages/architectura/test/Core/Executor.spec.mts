import { describe, it } from "node:test";

import { Executor } from "../../src/_index.mjs";

describe(
    "Executor",
    (): void =>
    {
        it.todo(
            "should be tested",
            (): void =>
            {
                Executor;
            }
        );
    }
);
