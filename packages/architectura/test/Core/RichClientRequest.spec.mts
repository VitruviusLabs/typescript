import { describe, it } from "node:test";

import { RichClientRequest } from "../../src/index.mjs";

describe(
    "RichClientRequest",
    (): void =>
    {
        it.todo(
            "should be tested",
            (): void =>
            {
                RichClientRequest;
            }
        );
    }
);
