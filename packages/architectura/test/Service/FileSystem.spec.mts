import { describe, it } from "node:test";

import { FileSystem } from "../../src/index.mjs";

describe(
    "FileSystem",
    (): void =>
    {
        it.todo(
            "should be tested",
            (): void =>
            {
                FileSystem;
            }
        );
    }
);
