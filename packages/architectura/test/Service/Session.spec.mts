import { describe, it } from "node:test";

import { Session } from "../../src/_index.mjs";

describe(
    "Session",
    (): void =>
    {
        it.todo(
            "should be tested",
            (): void =>
            {
                Session;
            }
        );
    }
);
