import { describe, it } from "node:test";

import { SessionManager } from "../../src/index.mjs";

describe(
    "SessionManager",
    (): void =>
    {
        it.todo(
            "should be tested",
            (): void =>
            {
                SessionManager;
            }
        );
    }
);
