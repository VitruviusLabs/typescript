import { describe, it } from "node:test";

import { SessionManager } from "../../src/_index.mjs";

describe(
    "SessionManager",
    (): void =>
    {
        it.todo(
            "should be tested",
            (): void =>
            {
                SessionManager;
            }
        );
    }
);
