import { describe, it } from "node:test";

describe("DomainService", (): void => {
	describe("LoadFromDirectory", (): void => {
		it.skip("should throw an error if no domain is found in the directory", (): void => {});
		it.skip("should load a domain from a directory", (): void => {});
	});

	describe("LoadMultipleFromRootDirectory", (): void => {
		it.skip("should load a domain from each sub-directory if it founds one", (): void => {});
	});
});
