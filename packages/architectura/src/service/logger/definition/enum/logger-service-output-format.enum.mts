const enum LoggerServiceOutputFormatEnum
{
	// eslint-disable-next-line @ts/no-shadow -- This is a false positive. A const enum can never shadow a global variable.
	JSON = "json",
	PLAIN = "plain",
}

export { LoggerServiceOutputFormatEnum };
